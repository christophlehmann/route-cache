{
    "name": "christophlehmann/route-cache",
    "description": "A cache implementation for record slugs that prevents fetching and overlaying records again in the PersistedAliasMapper",
    "type": "typo3-cms-extension",
    "license": [
        "GPL-2.0-or-later"
    ],
    "keywords": [
        "performance",
        "cache"
    ],
    "require": {
        "typo3/cms-core": "^12.4 || ^13.4"
    },
    "autoload": {
        "psr-4": {
            "Lemming\\RouteCache\\": "Classes/"
        }
    },
    "extra": {
        "typo3/cms": {
            "extension-key": "route_cache"
        }
    }
}